# 2026-08-29 홈 UI — Forest Hospital (`doc/design/02.png`)

배포 HTTPS 스모크 **15 passed** (`https://boardroom-six-delta.vercel.app`, 커밋 `cfd55fd`). 유효 안건으로 토론 시작은 누르지 않음.

## 이번에 한 일

- 중앙 장면: `public/forest-room.png` (너구리 재무이사 · 여우 마케팅실장 · 고양이 진료원장, 육각 테이블 중앙은 비움).
- 홈 셸: 로고+글자 메뉴 사이드바, 헤더, 회의실, 하단 라운드 타임라인. 시각 기준 `doc/design/02.png`.
- 업로드 성공 시 흰 종이 조각이 한 장씩 떨어진 뒤 `public/clay-paper-stack.png`(02와 같은 흰 종이산+민트 표 아이콘)가 테이블에 쌓이고, 노란 스파클이 터진 뒤 계속 반짝인다.
- 토론 결과·메모·공유는 장면 아래 접이식 패널. API·타임아웃·3사 provider 유지.

## 업로드 스택·스파클

1. 점선 드롭존 또는 `#metrics-file`로 CSV/XLSX.
2. `POST /api/metrics/parse` 200이면 목록 한 줄 + `clay-rise`로 점토 더미 등장 + `sparkle-burst`.
3. 마지막 성공 파싱의 `metrics`만 세션 POST에 실음.

## E2E 셀렉터 (유지)

`#agenda`, `#metrics-file`, 버튼 `토론 시작`, 디스클레이머, 더미 CSV/엑셀 링크, 안건 10–200자 문구.

로컬에서 더미 CSV 업로드 후 말풍선 3개·파일 1개·점토 더미·스파클을 확인. **유효 안건으로 토론 시작은 누르지 않음.**

## 02.png와 아직 다른 점

- 회의실·종이더미는 생성 에셋이라 02 픽셀과 1:1은 아님. 안건/`토론 시작`은 제품용으로 업로드 패널에 있음.
- 대시보드·설정·알림·「7일 남음」은 장식. 말풍선 좌표는 뷰포트에 따라 수 mm 어긋날 수 있음.

## HTTPS E2E

`pnpm test:e2e` 15 passed. baseURL `https://boardroom-six-delta.vercel.app`. 홈에서 포레스트 병원·파일 선택·회의 나가기·더미 CSV/엑셀 확인. 프로덕션에서 더미 CSV 업로드 후 말풍선·종이산·스파클 확인.
