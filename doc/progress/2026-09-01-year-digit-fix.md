# 2026-09-01 테이블 서류 「2026」 깨짐 수정

큰 ImageMagick `2026`을 구워진 `2024` 위에 올려 4가 비치고 `년`이 뭉개졌다.

## 10단계

1. 원인은 오버레이 크기·위치이지 폰트 파일이 아니다.
2. 깨진 PNG를 고치지 않고 `79b4bfe`의 `public/boardroom-room.png`로 되돌린다.
3. 손익계산서 `4`는 약 (241–250, 552–558), 타당성 검토 `4`는 약 (1220–1225, 566–572).
4. `년`·제목·앞자리 `202`는 마스크 밖이라 건드리지 않는다.
5. 12px 이상 글자를 올리면 다시 깨진다. 원래 숫자는 6–11px다.
6. `4` 먹만 종이색으로 메운다.
7. 같은 칸에 `6`을 그린다. 왼쪽은 검정, 오른쪽은 파란 `202`와 같은 먹.
8. 픽셀 비교: `년` 0, 손익 제목 0, 타당성 제목 0, 앞자리 `202` 0.
9. `?v=03y2026b`로 캐시를 깬다.
10. 커밋·푸시 후 프로덕션 PNG를 확인한다. 실 LLM 버튼은 누르지 않는다.

## 한 일

- `public/boardroom-room.png` 마지막 숫자만 4→6.
- `components/meeting-scene.tsx` 캐시 `03y2026b`.

## 검증

- 로컬: 제목·`년`·`202` 픽셀 불변, `4` 가로획이 `6` 열린 형태로 바뀜.
- HTTPS 화면은 배포 후. 실 LLM 버튼은 누르지 않음.
