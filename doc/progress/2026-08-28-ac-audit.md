# 2026-08-28 PRD AC 감사 (Executor)

배포: https://boardroom-six-delta.vercel.app  
앱 코드 HEAD 실측 기준: `7eda3e3`. 이 문서는 테스트·E2E·5안건 1회 실측을 추가한다. 키 값은 적지 않음.

전체 목표를 완료로 단정하지 않음. 아래는 항목별 증거만.

## 이번에 한 일

- E2E에서 프로덕션에 토론을 켜 버리는 “DB 미연결” 클릭 테스트를 제거.
- F2 201자 클라이언트, 세션 API 400, 409/422, `/s/uE7m2G` 메모, `/s/LNIDoe` 발언 불가, keepalive 401을 Playwright에 추가.
- F1 스키마·1000토큰, F5 3사 provider, 22/28/60 리터럴, `x-forwarded-for` 단위 테스트 보강.
- `pnpm test` 34 passed. `pnpm build` 통과. `pnpm test:e2e` 11 passed (baseURL HTTPS만).
- W3 안건 5종×1회 HTTPS eval (`scripts/eval-agendas.mjs` RUNS=1). 5×10은 하지 않음.

## F1–F6 / W1–W4

| 항목 | 판정 | 증거 |
|---|---|---|
| F1 지표 스키마·≤1000토큰 | 완료 | `lib/schema.ts` `MetricsSchema`. `instrumentation.ts` 기동 시 `loadMetrics`+표. 단위 테스트 `lib/schema.test.ts` metrics. |
| F2 안건 10–200 클라+서버 | 완료 | 클라 `lib/agenda.ts`+버튼 비활성. 서버 `SessionCreateSchema` 400. E2E 짧음/201자·POST 400. HTTPS 실측 400. |
| F3 30초 안 3셀 | 완료 | 세션 `uE7m2G` R1 9709ms ok=3. 이번 eval 5세션 R1 모두 ok=3·7388–9782ms. 1실패 UI: `/s/LNIDoe` “발언 불가” 2칸 E2E. |
| F4 R2 objection + R1<2 → 422 | 부분 | 성공 셀 objection: `uE7m2G` R2 ok=3, `MJg8Zz` R2 ok=3. 422: `LNIDoe` POST R2 → 422 `round1_insufficient` (E2E). 이번 5안건 중 4건은 Haiku R2가 빈 objection으로 **failed**(성공 셀은 objection 있음). |
| F5 3사 배지 + 빌드 throw | 완료 | `config/personas.ts` provider 중복 시 throw. 단위 테스트 3 provider. `/s/w4demo`·`/s/uE7m2G` E2E Anthropic/OpenAI/Google. |
| F6 메모 PUT 후 `/s/[id]` 비로그인 | 완료 | `uE7m2G` PUT 200 + 공유 페이지 합의점. 이번 `yl91gj` PUT 200, `/s/yl91gj` 메모 문구·password 입력 없음. |
| 제한 409/429 | 부분 | 409: `uE7m2G` R1 재요청 E2E 409. 429: `wouldExceed(10)` 단위 테스트. IP 11번째 HTTPS는 이번 턴에 안 씀. |
| W1 리포·모델·스키마 | 완료 | Next 15 루트. haiku / gpt-5.4-nano / flash-lite. health 4 true. |
| W2 UI·세션·메모·제한 | 완료 | `/` 그리드·메모. `/s/[id]`. 위 F1–F6 코드 경로. |
| W3 실패·프롬프트·keepalive·한도 | 부분 | 실패 셀·재시도·jsonrepair 있음. keepalive 워크플로 파일+무단 401. 주 2회 성공 로그는 gh 미인증으로 미확인. eval 5×1만(5×10 아님). Haiku R2 빈 objection 재발. |
| W4 발표/데모 | 완료 | `/demo/slide`, `/s/w4demo`(실호출 아님 고지), `doc/demo/90s-script.md`. E2E 슬라이드·w4demo. 실토론 대체 아님. |

## 이번 eval (RUNS=1, HTTPS)

| 세션 | 안건 | R1 ok / ms | R2 |
|---|---|---|---|
| `yl91gj` | 스마일 전용 레이저 리스 계약 12개월 | 3 / 9782 | cfo failed(빈 objection), mkt·md ok. 메모 PUT 200 |
| `ZxiwXC` | 백내장 검색광고 예산 30% 증액 | 3 / 8226 | cfo failed, mkt·md ok |
| `MJg8Zz` | 상담 간호사 1명 충원으로 설명 시간 확보 | 3 / 7785 | ok=3 objection 있음 |
| `6FWsGv` | ICL 패키지 가격 8퍼센트 인하 | 3 / 8566 | cfo failed, mkt·md ok |
| `PGzDOA` | 중국 에이전시 수수료 구간 상향 검토 | 3 / 7388 | cfo failed, mkt·md ok |

## 다음 Executor 한 가지

Haiku(cfo) 라운드 2가 빈 `objection`/`changed`로 셀을 버리는 비율이 높음(이번 5안건 중 4). 경량 모델 유지. 프롬프트·재시도만 손본다.
