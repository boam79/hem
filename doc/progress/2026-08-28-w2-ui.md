# 2026-08-28 W2 UI · 데모 백업

## 한 일

- shadcn (card, badge, textarea, select, button, skeleton)으로 `/` 그리드·메모 4항목·셀 skeleton
- `/s/[id]` 공유 페이지를 그리드·메모 읽기 전용으로 맞춤
- 사전 계산 백업 `/s/w4demo` (DB·키 없이도 F5 배지·R2 objection 시연)
- `GET /api/health` 키 존재 여부만 반환
- Playwright 스모크는 배포 HTTPS만 (`playwright.config.ts`가 localhost를 거부)

## 아직 막힌 것

- Supabase Free 활성 2/2. 새 프로젝트 월 비용 조회는 $0. `boam79_patient_data`는 합치지 않음. pause 대상은 사용자 선택 필요 (`qr-asset-manager` 후보).
- 3사 API 키가 로컬·Vercel에 없음 → W1-3 모델 ID 실측, AC F3 실토론 30초는 미검증.

## HTTPS 스모크 (2026-08-28, `2a493d3` READY)

- Playwright 3/3 통과. baseURL `https://boardroom-six-delta.vercel.app` (localhost 아님).
- `/s/w4demo` 공개, Anthropic/OpenAI/Google 배지, R2 반대 3개.
- `GET /api/health`: anthropic/openai/google/supabase 모두 `false`. healthcheck exit 2.
