# 2026-08-28 W3/W4 게이트·슬라이드

## 한 일

- F4 `canStartRound2` 단위 테스트, 라운드 라우트에 연결
- 홈에 라운드 1 지연(ms) 표시 — 키가 붙으면 F3 30초를 화면에서 확인
- `scripts/eval-agendas.mjs` — 배포 HTTPS만, health가 모두 true일 때만 실행
- `/demo/slide` Du·PoLL 한 장
- 공개 문서 기준 모델 ID: `gpt-5.4-mini`, `gemini-3.1-flash-lite` (우리 키로 JSON 실측은 아직 없음)

## 블로커 (실토론 AC)

- Vercel 프로젝트 env: **0개**. `vercel env ls` 확인.
- `/api/health` anthropic/openai/google/supabase 전부 false.
- Supabase org `free`, 활성 2/2 (`qr-asset-manager`, `boam79_patient_data`). 환자 DB 재사용 금지. 새 프로젝트 월 비용 $0이나 칸이 없음.

다음에 필요한 사용자 결정: `qr-asset-manager`를 pause해도 되는지. 그 다음 Boardroom 프로젝트 생성 → SQL 적용 → Vercel에 키 5종.
