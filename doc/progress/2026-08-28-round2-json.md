# 2026-08-28 라운드 2 JSON·objection

배포: https://boardroom-six-delta.vercel.app  
코드: `7eda3e3` (production READY `dpl_DsCZZLzPi4iDqijHuHoGx9pZ8zD5`)  
키 값은 적지 않음. 경량 3사 유지: haiku / gpt-5.4-nano / flash-lite.

## 원인

세션 `4fyIcc` R1은 ok=3이었으나 같은 세션 R2는 ok=1.

- cfo(Haiku): `Colon expected at position 570`. jsonrepair는 전각 콜론(`：`)과 `"key" 한글값`처럼 `:`가 빠진 객체를 고치지 못하고 던진다.
- mkt(nano): `objection` 빈 문자열 → Zod `too_small`. R2 재시도 예시가 R1 모양이라 objection/changed를 비우거나 생략했다. 토큰 한도에 걸리면 뒤쪽 필드가 빈 값이 되기 쉽다.

## 수정

- `lib/json.ts`: 전각 콜론을 `:`로 바꾸고, `"key" 한글` 사이에 `:`를 넣은 뒤 jsonrepair. `Colon expected`면 해당 위치에 `:`를 넣고 한 번 더.
- `lib/schema.ts`: R2 LLM 스키마에서 objection·changed를 앞에 둠. 공란·공백만이면 실패로 재시도.
- `lib/prompt.ts` `jsonOnlySuffix(2)`: objection/changed가 있는 예시. `config/personas.ts` ROUND2_RULES에 빈 문자열 금지.

R1 스키마·경량 모델 ID·`maxDuration = 60`은 그대로.

## 단위 테스트

`pnpm test` 30 passed. 픽스처: 전각 콜론, 키 뒤 한국어 값 콜론 누락, 빈/공백 objection 거부, R2 JSON 예시에 objection. `pnpm build` 통과.

## HTTPS 새 세션 (`4fyIcc`는 R2가 이미 있어 409)

안건: 백내장 검색광고 예산 30% 증액  
세션: `uE7m2G`  
공유: https://boardroom-six-delta.vercel.app/s/uE7m2G

### 라운드 1 — 벽시계 9709ms, HTTP 200, okCount=3

| 셀 | provider | modelId | status | latencyMs | 에러 |
|---|---|---|---|---|---|
| cfo | anthropic | `claude-haiku-4-5-20251001` | ok | 5205 | 없음 |
| mkt | openai | `gpt-5.4-nano` | ok | 3096 | 없음 |
| md | google | `gemini-3.1-flash-lite` | ok | 1398 | 없음 |

### 라운드 2 — 벽시계 10963ms, HTTP 200, okCount=3

| 셀 | status | latencyMs | objection | 에러 |
|---|---|---|---|---|
| cfo | ok | 7783 | 비어 있지 않음 (147자) | 없음 |
| mkt | ok | 4592 | 비어 있지 않음 (200자) | 없음 |
| md | ok | 1905 | 비어 있지 않음 (56자) | 없음 |

메모 PUT HTTP 200.

## 사용자에게

완료로 단정하지 않음. 사이트에서 세션 `uE7m2G` 라운드 2 세 칸에 반대(objection)가 보이는지 확인해 주세요.
