# 2026-08-31 홈 UI — Boardroom 03 (`doc/design/03.png`)

사용자가 02 포레스트(민트·점토 동물) 대신 **3번 시안**으로 바꾸라고 했다.

## 한 일

- 셸: Boardroom 워드마크, 파란 십자가, 네이비/화이트. 사이드바 6칸은 유지(03 목업에 없어도 제품 내비).
- 헤더: 「시뮬레이션 진행 중」·설정·「시뮬레이션 다음 턴」. 버튼 보이는 글자는 03, accessible name은 `토론 시작`(E2E·안건 10자 규칙).
- 홈 왼쪽: 회의 안건 + 지표 업로드(`#home-metrics-file`) + 데이터 검토. `/files`의 `#metrics-file`은 그대로.
- 회의실: `public/boardroom-room.png`(사람 3명, 빈 테이블). 업로드 후 SVG 서류 6장(손익 막대·비용 파이·외래 선·진료과·마케팅·인력)과 네이비/코랄/틸 말풍선.
- 하단 도크: 회의록 `/debate` · 지표 대시보드 `/dashboard` · 시나리오 결과 `/decision` · AI 인사이트 `/debate`(4번째 LLM 없음).

## 검증

- 단위 117. `pnpm build` 통과.
- HTTPS E2E는 배포 후. 「토론 시작」「데이터 검토」실클릭 없음.

## 아직 03과 다른 점

- 안건 한도는 PRD대로 200자(목업 500 아님).
- 왼쪽 6칸 메뉴는 목업에 없고 제품에 있음.
- 비용 파이·마케팅 파이는 스키마에 인건비 항이 없어 매출 구성·유입 채널을 쓴다.
