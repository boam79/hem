# 2026-08-29 파일 관리 전용 · 대기 말풍선 · 대시보드 비용 분리

## 이번에 한 일

- 사이드바 **파일 관리**는 `/files`. CSV/XLSX 업로드·목록·더미 다운로드만. 원본은 저장하지 않고 파싱 JSON만 `localStorage`에 두어 홈 토론이 쓴다.
- 홈에서 파일 피커를 뺐다. 안건·유형·토론 시작 + 회의실.
- 토론 시작 전 말풍선 3개(`자료를 올려 주세요.`)를 없애고 테이블 가운데 한 줄만 표시.
- 이름표(재무이사·마케팅실장·진료원장)를 각 캐릭터 가슴 쪽으로 옮김.
- 대시보드에서 **비용 대시보드** 제거. 설정에만 남김.

## 검증

- 단위 `pnpm test` 84, `pnpm build` `/files` 라우트.
- HTTPS E2E는 배포 후. 유효 안건으로 토론 시작은 누르지 않음.
